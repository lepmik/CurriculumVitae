% RCN Early Career CV — matches "template-for-cv-early-career-scientists" DOCX format
% All content sourced from data/*.yaml — no hardcoding
% Auto-generated by cv.py — DO NOT EDIT manually
\documentclass[a4paper,11pt]{article}
\usepackage{style}

\begin{document}

% ── Title ─────────────────────────────────────────────────────────────────────
{\Large\bfseries Curriculum vitae with early achievements track record}
{\normalsize\bfseries (for early career scientists)}

\bigskip

% ── Role in the project ───────────────────────────────────────────────────────
\noindent\textbf{Role in the project}\quad
Project manager $\boxtimes$\qquad
Project participant $\square$

\medskip

% ── Personal information ──────────────────────────────────────────────────────
\section*{Personal information}

\begin{rcninfo}
  First name, Surname: & \VAR{profile.name} \\\hline
  \multicolumn{2}{|l|}{Date of birth:\quad \VAR{profile.date_of_birth | default('')} \hfill Sex:\quad Male} \\\hline
  Nationality: & \VAR{profile.nationality} \\\hline
  Researcher unique identifier(s) (ORCID, ResearcherID, etc.): & \VAR{profile.orcid} \\\hline
  URL for personal website: &
    \href{\VAR{profile.website_institution}}{\VAR{profile.website_institution}};
    \href{\VAR{profile.website}}{\VAR{profile.website}};
    \href{https://scholar.google.no/citations?user=\VAR{profile.scholar_id}}{scholar.google.no} \\
\end{rcninfo}

% ── Education ─────────────────────────────────────────────────────────────────
\section*{Education}

\begin{rcntable}{Year}{Faculty/department, University/institution, Country}
\BLOCK{ for edu in education }
  \VAR{edu.end} &
  \textbf{\VAR{edu.degree}}\BLOCK{ if edu.get('department') }, \VAR{edu.department}\BLOCK{ endif }, \VAR{edu.institution}\BLOCK{ if edu.get('location') }, \VAR{edu.location}\BLOCK{ endif } \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Career breaks ─────────────────────────────────────────────────────────────
\section*{Career breaks}

\begin{rcntable}{Year}{Reason}
\BLOCK{ for cb in narrative.career_breaks }
  \VAR{cb.year} & \VAR{cb.reason} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Subtracting time from age / period after doctoral defence ─────────────────
\BLOCK{ if narrative.doctoral_defence_date }
\section*{Subtracting time from age/period after doctoral defence}

\begin{rcninfo}
  Date of doctoral defence: & \VAR{narrative.doctoral_defence_date} \\\hline
\end{rcninfo}
\BLOCK{ endif }

% ── Positions ─────────────────────────────────────────────────────────────────
\section*{Positions (academic, business, industry, public sector, national or international organisations)}

\begin{rcntable}{Year}{Job title, Employer, Country}
\BLOCK{ for exp in experience }
  \VAR{exp.start}--\VAR{exp.end} &
  \textbf{\VAR{exp.title}}\BLOCK{ if exp.get('subtitle') } (\VAR{exp.subtitle | latex_escape})\BLOCK{ endif },
  \BLOCK{ if exp.get('department') }\VAR{exp.department}, \BLOCK{ endif }\VAR{exp.institution}, \VAR{exp.location} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Fellowships, Awards and Prizes ────────────────────────────────────────────
\section*{Fellowships, awards and prizes}

\begin{rcntable}{Year}{Name of institution/country}
\BLOCK{ for award in awards }
  \VAR{award.year} & \VAR{award.title}, \VAR{award.issuer} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Mobility ──────────────────────────────────────────────────────────────────
\section*{Mobility}

\begin{rcntable}{Year}{Name of faculty/department/centre, university/institution, country}
\BLOCK{ for m in mobility }
  \VAR{m.start}\BLOCK{ if m.end and m.end != m.start }/\VAR{m.end}\BLOCK{ endif } &
  \textbf{\VAR{m.title}}\BLOCK{ if m.get('subtitle') } (\VAR{m.subtitle})\BLOCK{ endif },
  \BLOCK{ if m.get('department') }\VAR{m.department}, \BLOCK{ endif }\VAR{m.institution}, \VAR{m.location} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Project management experience ─────────────────────────────────────────────
\section*{Project management experience}

\begin{rcntable}{Year}{Project and role, funding from}
\BLOCK{ for grant in grants }
\BLOCK{ if grant.status == "funded" }
  \VAR{grant.start}\BLOCK{ if grant.get('end') and grant.end and grant.end != 'present' }--\VAR{grant.end}\BLOCK{ endif } &
  \VAR{grant.funder}, \textbf{\VAR{grant.title}},
  \textit{\VAR{grant.role}}, \VAR{grant.amount}\BLOCK{ if grant.get('description') } --- \VAR{grant.description}\BLOCK{ endif } \\\hline
\BLOCK{ endif }
\BLOCK{ endfor }
\end{rcntable}

% ── Supervision of graduate students and research fellows ─────────────────────
\section*{Supervision of graduate students and research fellows}

\BLOCK{ if supervision }
\BLOCK{ set phd_main = supervision.phd | selectattr('role','equalto','Main supervisor') | list if supervision.get('phd') else [] }
\BLOCK{ set phd_co   = supervision.phd | selectattr('role','equalto','Co-supervisor')   | list if supervision.get('phd') else [] }
\BLOCK{ set n_fin      = phd_main | selectattr('status','equalto','finished') | list | length }
\BLOCK{ set n_co_fin   = phd_co   | selectattr('status','equalto','finished') | list | length }
\BLOCK{ set n_masters = supervision.masters | map(attribute='count') | sum if supervision.get('masters') else 0 }
\BLOCK{ set n_postdoc = supervision.postdoc | length if supervision.get('postdoc') else 0 }
\begin{rcnsupervision}
  \multirow{2}{*}{\VAR{n_masters}} &
  \VAR{phd_main|length} as Main supervisor (\VAR{n_fin} finished) &
  \multirow{2}{\hsize}{\RaggedRight \VAR{narrative.supervision_institutions}} \\\cline{2-2}
  & \VAR{phd_co|length} as Co-supervisor (\VAR{n_co_fin} finished) & \\\hline
\end{rcnsupervision}
\BLOCK{ endif }

% ── Teaching activities ───────────────────────────────────────────────────────
\section*{Teaching activities}

\begin{rcntable}{Year}{Teaching position -- topic, university/institution, country}
\BLOCK{ for t in teaching }
  \VAR{t.year} &
  \textit{\VAR{t.role}}, \VAR{t.course}, \VAR{t.institution}, \VAR{t.location}\BLOCK{ if t.get('description') } --- \VAR{t.description}\BLOCK{ endif } \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Organisation of meetings ──────────────────────────────────────────────────
\section*{Organisation of meetings}

\begin{rcntable}{Year}{Role and name of event/number of participants/country}
\BLOCK{ for s in service | selectattr('category','equalto','meeting_org') }
  \VAR{s.year} & \VAR{s.description} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Institutional responsibilities ────────────────────────────────────────────
\section*{Institutional responsibilities}

\begin{rcntable}{Year}{Role, university/institution, country}
\BLOCK{ for s in service | selectattr('category','equalto','institutional') }
  \VAR{s.year} & \VAR{s.description} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Commissions of trust ──────────────────────────────────────────────────────
\section*{Commissions of trust in academic, public or private organisations}

\begin{rcntable}{Year}{Role, university/institution, country}
\BLOCK{ for s in service | selectattr('category','equalto','trust') }
  \VAR{s.year} & \VAR{s.description} \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Memberships / Networks ────────────────────────────────────────────────────
\section*{Memberships of academies / scientific societies / networks}

\begin{rcntable}{Year}{Name of academy, scientific society, network}
\BLOCK{ for m in memberships }
  \VAR{m.start}\BLOCK{ if m.end and m.end != 'present' }--\VAR{m.end}\BLOCK{ endif }\BLOCK{ if m.end == 'present' }--\BLOCK{ endif } &
  \VAR{m.name}\BLOCK{ if m.get('description') }, \VAR{m.description}\BLOCK{ endif },
  \textit{\VAR{m.role}}\BLOCK{ if m.get('url') } (\href{\VAR{m.url}}{\VAR{m.url}})\BLOCK{ endif } \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Major collaborations ──────────────────────────────────────────────────────
\section*{Major collaborations}

\begin{rcntable}{Year}{University/institution, Topic}
\BLOCK{ for c in collaborations }
  \VAR{c.start}\BLOCK{ if c.end and c.end not in ['present', ''] }--\VAR{c.end}\BLOCK{ elif c.end == 'present' }--\BLOCK{ endif } &
  \VAR{c.description}, \VAR{c.collaborators}\BLOCK{ if c.get('role') }, \textit{\VAR{c.role}}\BLOCK{ endif } \\\hline
\BLOCK{ endfor }
\end{rcntable}

% ── Early achievements track record ───────────────────────────────────────────
\section*{Early achievements track record}

\BLOCK{ set journals  = publications | selectattr("type","equalto","journal")    | sort(attribute="year",reverse=True) | list }
\BLOCK{ set confs     = publications | selectattr("type","equalto","conference") | sort(attribute="year",reverse=True) | list }
\BLOCK{ set preprints = publications | selectattr("type","equalto","preprint")   | sort(attribute="year",reverse=True) | list }

\BLOCK{ set nca_feat   = publications | selectattr("featured_earlycareer") | selectattr("rcn_group","defined") | selectattr("rcn_group","equalto","nca")          | sort(attribute="rcn_num") | list }
\BLOCK{ set spnav_feat = publications | selectattr("featured_earlycareer") | selectattr("rcn_group","defined") | selectattr("rcn_group","equalto","spatial_nav")   | sort(attribute="rcn_num") | list }
\BLOCK{ set causal_feat= publications | selectattr("featured_earlycareer") | selectattr("rcn_group","defined") | selectattr("rcn_group","equalto","causal")        | sort(attribute="rcn_num") | list }
\BLOCK{ set neuro_feat = publications | selectattr("featured_earlycareer") | selectattr("rcn_group","defined") | selectattr("rcn_group","equalto","neuroscience")  | sort(attribute="rcn_num") | list }

\BLOCK{ set start_spnav  = 1 + nca_feat|length }
\BLOCK{ set start_causal = start_spnav + spnav_feat|length }
\BLOCK{ set start_neuro  = start_causal + causal_feat|length }

{\small

I have published \textbf{\VAR{journals|length + confs|length}} articles in peer-reviewed journals and conference proceedings\BLOCK{ if scholar_stats and scholar_stats.get('h_index') },
h-index \textbf{\VAR{scholar_stats.h_index}} (\VAR{scholar_stats.total_citations} total citations, Google Scholar \VAR{scholar_stats.fetched_date})\BLOCK{ endif }.
In addition, I have \textbf{\VAR{preprints|length}} preprints currently under review.

\VAR{narrative.ec_track_record_intro}


\BLOCK{ if nca_feat }
\subsubsection*{Selected publications: Neural cellular automata (NCA)}

\VAR{narrative.ec_pub_narrative_nca}

\begin{enumerate}[leftmargin=*, label={[\arabic*]}, itemsep=1pt, topsep=2pt, start=1]
\BLOCK{ for pub in nca_feat }
  \item \VAR{pub.authors | abbrev_authors}. \VAR{pub.title}.\BLOCK{ if pub.get('rcn_note') } (\VAR{pub.rcn_note})\BLOCK{ endif }
    \textit{\VAR{pub.venue}}, \VAR{pub.year}.\BLOCK{ if pub.get('citations') } \textbf{\VAR{pub.citations} cit.}\BLOCK{ endif }
\BLOCK{ endfor }
\end{enumerate}
\BLOCK{ endif }

\BLOCK{ if spnav_feat }
\subsubsection*{Selected publications: Cognitive Maps in Spatial Navigation}

\VAR{narrative.ec_pub_narrative_spatial_nav}

\begin{enumerate}[leftmargin=*, label={[\arabic*]}, itemsep=1pt, topsep=2pt, start=\VAR{start_spnav}]
\BLOCK{ for pub in spnav_feat }
  \item \VAR{pub.authors | abbrev_authors}. \VAR{pub.title}.
    \textit{\VAR{pub.venue}}, \VAR{pub.year}.\BLOCK{ if pub.get('citations') } \textbf{\VAR{pub.citations} cit.}\BLOCK{ endif }
\BLOCK{ endfor }
\end{enumerate}
\BLOCK{ endif }

\BLOCK{ if causal_feat }
\subsubsection*{Selected publications: Causal inference}

\VAR{narrative.ec_pub_narrative_causal}

\begin{enumerate}[leftmargin=*, label={[\arabic*]}, itemsep=1pt, topsep=2pt, start=\VAR{start_causal}]
\BLOCK{ for pub in causal_feat }
  \item \VAR{pub.authors | abbrev_authors}. \VAR{pub.title}.
    \textit{\VAR{pub.venue}}, \VAR{pub.year}.\BLOCK{ if pub.get('citations') } \textbf{\VAR{pub.citations} cit.}\BLOCK{ endif }
\BLOCK{ endfor }
\end{enumerate}
\BLOCK{ endif }

\BLOCK{ if neuro_feat }
\subsubsection*{Selected publications: Neuroscience}

\VAR{narrative.ec_pub_narrative_neuroscience}

\begin{enumerate}[leftmargin=*, label={[\arabic*]}, itemsep=1pt, topsep=2pt, start=\VAR{start_neuro}]
\BLOCK{ for pub in neuro_feat }
  \item \VAR{pub.authors | abbrev_authors}. \VAR{pub.title}.\BLOCK{ if pub.get('note') } \VAR{pub.note}.\BLOCK{ endif }
    \textit{\VAR{pub.venue}}, \VAR{pub.year}.\BLOCK{ if pub.get('citations') } \textbf{\VAR{pub.citations} cit.}\BLOCK{ endif }
\BLOCK{ endfor }
\end{enumerate}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_monographs }
\bigskip
\noindent\textbf{Research monographs and translations.}
\VAR{narrative.early_career_monographs}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_patents }
\bigskip
\noindent\textbf{Granted patent(s).}
\VAR{narrative.early_career_patents}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_innovation }
\bigskip
\noindent\textbf{Leadership/participation in industrial or public innovation or societal impact.}
\VAR{narrative.early_career_innovation}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_fieldwork }
\bigskip
\noindent\textbf{Leadership of field work or research expeditions.}
\VAR{narrative.early_career_fieldwork}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_invited_talks }
\bigskip
\noindent\textbf{Invited presentations to peer-reviewed national or international conferences and/or international advanced schools.}
\VAR{narrative.early_career_invited_talks}
\BLOCK{ endif }

\BLOCK{ if narrative.early_career_outreach }
\bigskip
\noindent\textbf{Experiences from major research communication, dissemination or outreach activities.}
\VAR{narrative.early_career_outreach}
\BLOCK{ endif }

}

\vfill
\begin{flushright}\tiny Generated \VAR{build_date}\end{flushright}

\end{document}
